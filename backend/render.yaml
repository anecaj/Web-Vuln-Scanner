services:
  - type: web
    name: web-vuln-scanner-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn app:app --workers 1 --bind 0.0.0.0:$PORT --timeout 120 --preload
